# 《解》待实现事项 · 销项清单

> **销项规则**：本清单只列「尚未实现」的事项，按技术类型分三章节。**完成一项即删除一项**，不在清单内保留已完成记录。
> 最后更新：2026-07-29
> 本次销项：删 §二.1（主角色行走精灵表已接入 80×120×8帧/向 × 3朝向 × 男女）

## 一、后端代码（游戏功能 / 服务端逻辑）

1. **坐骑系统**
   - 现状：无任何 `mount` / 坐骑 实现（全局搜索 0 命中）
   - 内容：玩家骑乘移动、坐骑属性 / 外观加成

## 二、美术资源（下列均为程序化占位或完全缺失；已接入真实美术的不列入）

> **已确认不缺口**：斩魄刀立绘(72)、状态/元素/材料/消耗品/装备槽图标、鬼道 TAB+节点(63)、21 区域背景、城镇/战斗背景、采集点、副本传送阵、立绘粒子(4)。

1. **怪物精灵**
   - 现状：`enemy_small` / `enemy_elite` / `enemy_boss` 三类**永远为程序化红块占位**（不在 assetManifest，BootScene 强制生成），`enemy.png` 仅通用占位
   - 内容：按怪物种类出美术（杂兵 / 精英 / 各区域 Boss 独立精灵或立绘）

3. **NPC 精灵**
   - 现状：`npc` **永远为程序化绿块占位**（不在 assetManifest），所有对话 NPC 共用同一色块
   - 内容：按 NPC 种类（铁匠 / 商店 / 任务发布者等）出独立美术

4. **Spine 骨骼动画**
   - 现状：全项目无任何 Spine 接入（`@esotericsoftware/spine-phaser` 未使用），玩家 / 怪物 / NPC 均无骨骼动画
   - 内容：美术产出骨骼资源 + 技术接入，替代静态 PNG / 程序化占位

5. **战斗界面 · 技能特效动画序列帧**
   - 现状：全项目无 spritesheet / 序列帧动画（grep `spritesheet|generateFrameNames|anims.create` 0 命中），技能施法 / 命中 / Buff 均为程序化 graphics 色块或纯逻辑
   - 内容：美术产出施法 / 命中 / 增益 / 减益 / 伤害的序列帧动画，接入 Phaser anims

## 三、上线部署运维（Stage E，可选）

1. **数据库迁移：SQLite → PostgreSQL**
   - 现状：当前本地 SQLite（better-sqlite3），未做 PG 适配
   - 内容：账号 / 世界状态表迁移至 PG，适配连接池与并发

2. **容器化：Docker**
   - 现状：未做
   - 内容：client + server + DB 容器编排，提供 docker-compose

3. **云主机部署（WebSocket 长连接）**
   - 现状：仅本地 `npm run dev`，未部署
   - 内容：云主机 + 反代（支持 Colyseus WebSocket 长连接）+ 进程守护
