# 《解》待实现事项 · 销项清单

> **销项规则**：本清单只列「尚未实现」的事项，按技术类型分章节。**完成一项即删除一项**，不在清单内保留已完成记录。
> 最后更新：2026-08-02

> **本轮已修复（非待实现项，仅记录供追溯）**：`entityNames.ts` 遗漏 7 个 `sys_*` 系统 NPC 显示名映射（`sys_return` / `sys_craft` / `sys_board` / `sys_portal_hueco` / `sys_portal_shadow` / `sys_hell_return` / `sys_abyss_return`），导致主世界传送门 / 制造阵 / 公告板等显示为英文 id。已补映射；并清理 21 个 `zone_*.ts` 中 63 处死 `name` 字段、259 处废弃 `dialogue.speaker` 字段；`GameScene` 头顶名牌 / 交互提示 / 对话框标题三处统一走 `npcDisplayName(id)`。改动已通过 `tsc --noEmit`（0 错）。

## 一、后端代码（游戏功能 / 服务端逻辑）

1. **坐骑系统**
   - 现状：无真实坐骑实现（全局搜索 `mount` / `坐骑` 命中的均为 `amount` / `remount` 等子串误报）
   - 内容：玩家骑乘移动、坐骑属性 / 外观加成
   - ⚠️ 待定：原始设定中坐骑归属《归一》，是否纳入《解》需你拍板；若不做则从清单移除

## 二、美术资源（程序化占位或完全缺失；已接入真实美术的不列入）

1. **战斗界面 · 技能特效动画序列帧**
   - 现状：全项目仅玩家行走一处用了 spritesheet + `anims.create`（`GameScene.ts:219/252`），**战斗内技能施法 / 命中 / Buff 仍为程序化 graphics 色块或纯逻辑**。四元素粒子 PNG（`fx_fire` / `fx_wind` / `fx_water` / `fx_earth`）已入 assetManifest，但目前只被属性面板（`StatPanel.ts:280`）消费，未接入战斗
   - 内容（两步走，先低成本见效）：
     ① **先接粒子**：用 Phaser particle emitter 把现有四元素粒子贴图合成施法 / 命中特效，即时改善战斗观感
     ② **再换序列帧**：美术产出正式 施法 / 命中 / 增益 / 减益 / 伤害 序列帧后接入
   - 接入点：`BattleFx.ts`（当前不存在，需新建，作为战斗特效唯一出口，避免特效逻辑散落各技能文件）

## 三、上线部署运维（Stage E，可选）

1. **数据库迁移：SQLite → PostgreSQL**
   - 现状：当前本地 SQLite（better-sqlite3），未做 PG 适配
   - 内容：账号 / 世界状态表迁移至 PG，适配连接池与并发
2. **容器化：Docker**
   - 现状：未做（无 Dockerfile / docker-compose）
   - 内容：client + server + DB 容器编排，提供 docker-compose
3. **云主机部署（WebSocket 长连接）**
   - 现状：仅本地 `npm run dev`，未部署
   - 内容：云主机 + 反代（支持 Colyseus WebSocket 长连接）+ 进程守护

---

### 远期规划（非当前迭代，仅备忘，不计入销项）
- 套装系统、PVP 竞技场、公会 / 拍卖行 / 好友系统（原始设计提及，优先级低于上面四项）
- 坐骑系统：原始设定归属《归一》，是否纳入《解》待定（见第一节）
