# 《解》待实现事项 · 销项清单

> **销项规则**：本清单只列「尚未实现」的事项，按技术类型分三章节。**完成一项即删除一项**，不在清单内保留已完成记录。
> 最后更新：2026-08-02

## 一、后端代码（游戏功能 / 服务端逻辑）

1. **坐骑系统**
   - 现状：无任何 `mount` / 坐骑 实现（全局搜索 0 命中）
   - 内容：玩家骑乘移动、坐骑属性 / 外观加成

## 二、美术资源（下列均为程序化占位或完全缺失；已接入真实美术的不列入）

1. **Spine 骨骼动画**
   - 现状：全项目无任何 Spine 接入（`@esotericsoftware/spine-phaser` 未使用）。玩家有 8 帧行走序列帧，NPC / 怪物 / Boss 均为静态 PNG 立绘，无骨骼动画
   - 内容：美术产出骨骼资源 + 技术接入，替代静态 PNG（NPC 已确认用静态 PNG 立绘，不走 Spine）

2. **战斗界面 · 技能特效动画序列帧**
   - 现状：全项目仅玩家行走一处用了 spritesheet + `anims.create`（`GameScene.ts:219/252`），**战斗内技能施法 / 命中 / Buff 仍为程序化 graphics 色块或纯逻辑**。四元素粒子 PNG（`fx_fire` / `fx_wind` / `fx_water` / `fx_earth`）已入 assetManifest，但目前只被属性面板（`StatPanel.ts:280`）消费，未接入战斗
   - 内容：① 美术产出施法 / 命中 / 增益 / 减益 / 伤害序列帧；② 接入 Phaser anims；③ 现有四元素粒子贴图接入战斗技能表现

## 三、上线部署运维（Stage E，可选）

1. **数据库迁移：SQLite → PostgreSQL**
   - 现状：当前本地 SQLite（better-sqlite3），未做 PG 适配
   - 内容：账号 / 世界状态表迁移至 PG，适配连接池与并发

2. **容器化：Docker**
   - 现状：未做（无 Dockerfile / docker-compose）
   - 内容：client + server + DB 容器编排，提供 docker-compose

3. **云主机部署（WebSocket 长连接）**
   - 现状：仅本地 `npm run dev`，未部署
   - 内容：云主机 + 反代（支持 Colyseus WebSocket 长连接）+ 进程守护
